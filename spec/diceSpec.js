describe("Dice", () => {
    it("Should return range of 1 - 6", () => {
        let die = new Die ();
        let pop = die.roll(6);
        expect(pop).toBe()
    });

    it("Should return range of error message", () => {
        let die = new Die ();
        let pop = die.roll();
        expect(pop).toBe()
    });

    it("Should return range of 1 - 6", () => {
        let die = new Die ();
        let pop = die.roll();
        expect(pop).toBe()
    });
})